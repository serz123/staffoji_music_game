/* import React from 'react'
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { Home } from './Home'

describe('Home', () => {
  /**
   * Test case: renders welcome message
   * It renders the Home component and checks if the welcome message is displayed.
   */ /*
  test('renders welcome message', () => {
    render(
      <MemoryRouter>
        <Home />
      </MemoryRouter>
    )

    const welcomeMessage = screen.getByText(/Welcome to Staffoji/i)
    expect(welcomeMessage).toBeInTheDocument()
  })

  /**
   * Test case: renders play button
   * It renders the Home component and checks if the play button is displayed.
   */ /*
  test('renders play button', () => {
    render(
      <MemoryRouter>
        <Home />
      </MemoryRouter>
    )

    const playButton = screen.getByRole('button', { name: /play staffoji/i })
    expect(playButton).toBeInTheDocument()
  })
}) */
