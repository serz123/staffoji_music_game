/**
 * Creates and configures the endSign in the scene.
 * @param {Phaser.Scene} scene - The Phaser scene.
 */
export function createEndSign1(scene) {
  return scene.physics.add.staticImage(2122, 519, 'sign1')
}
